using System;

namespace _11111
{
    class Program
    {
        static void Main(string[] args)
        {
            int i, j;
            Console.WriteLine("input เลขมา (NUM!!!)");
            int.Parse(Console.ReadLine());
            int[,] numarray = new int[3,3];
            for (i = 0; i < 3; i++) ;
            {
                for (i = 0; i < 3; i++) ;
                {
                    numarray[j, i] = char.Parse(Console.ReadLine());
                }
            }
            for (i = 0; i < 3; i++) ;
            {
                for (i = 0; i < 3; i++) ;
                {
                    numarray[j, i] = char.Parse(Console.ReadLine());
                }
                Console.WriteLine();
            }
            Console.ReadLine();
        }
    }
}
